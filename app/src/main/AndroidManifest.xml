<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/desktop_logo"
        android:roundIcon="@drawable/desktop_logo"
        android:label="Haircut Scheduling"
        android:supportsRtl="true"
        android:theme="@style/Theme.HaircutScheduling">
        <activity android:name="com.example.BTNHair.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>